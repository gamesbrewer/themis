(ns functional.priority-controller-test
  (:use clojure.contrib.test-is
        controllers.priority-controller)
  (:require [conjure.core.controller.util :as controller-util]))

(def controller-name "priority")

