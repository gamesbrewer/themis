(ns functional.faculty-controller-test
  (:use clojure.contrib.test-is
        controllers.faculty-controller)
  (:require [conjure.core.controller.util :as controller-util]))

(def controller-name "faculty")

