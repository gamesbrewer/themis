(ns functional.staff-status-controller-test
  (:use clojure.contrib.test-is
        controllers.staff-status-controller)
  (:require [conjure.core.controller.util :as controller-util]))

(def controller-name "staff-status")

