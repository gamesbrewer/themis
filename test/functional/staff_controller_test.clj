(ns functional.staff-controller-test
  (:use clojure.contrib.test-is
        controllers.staff-controller)
  (:require [conjure.core.controller.util :as controller-util]))

(def controller-name "staff")

