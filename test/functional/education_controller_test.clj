(ns functional.education-controller-test
  (:use clojure.contrib.test-is
        controllers.education-controller)
  (:require [conjure.core.controller.util :as controller-util]))

(def controller-name "education")

