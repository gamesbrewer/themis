(ns functional.position-status-controller-test
  (:use clojure.contrib.test-is
        controllers.position-status-controller)
  (:require [conjure.core.controller.util :as controller-util]))

(def controller-name "position-status")

