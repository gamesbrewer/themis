(ns functional.religion-controller-test
  (:use clojure.contrib.test-is
        controllers.religion-controller)
  (:require [conjure.core.controller.util :as controller-util]))

(def controller-name "religion")

