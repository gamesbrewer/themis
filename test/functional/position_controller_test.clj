(ns functional.position-controller-test
  (:use clojure.contrib.test-is
        controllers.position-controller)
  (:require [conjure.core.controller.util :as controller-util]))

(def controller-name "position")

