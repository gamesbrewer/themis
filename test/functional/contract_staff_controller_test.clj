(ns functional.contract-staff-controller-test
  (:use clojure.contrib.test-is
        controllers.contract-staff-controller)
  (:require [conjure.core.controller.util :as controller-util]))

(def controller-name "contract_staff")

