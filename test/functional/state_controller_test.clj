(ns functional.state-controller-test
  (:use clojure.contrib.test-is
        controllers.state-controller)
  (:require [conjure.core.controller.util :as controller-util]))

(def controller-name "state")

